# 参赛文档：AI工具生成平台（外部招商版）

## 1. 项目背景与目标
- 业务痛点：AI 工具碎片化、流程不可见、产出不一致、敏感信息裸露。
- 目标：通过「模板化任务 + 表单生成 + 一键调用」降低业务接入门槛，并满足外部招商、可公开部署（GitHub / Vercel）。

## 2. 本次迭代要点
- 去内网依赖：移除 @jd / @jdei / codmi 相关私有依赖与脚本，使用通用开源依赖，可直接在 GitHub / Vercel 构建。
- 安全合规：所有 Ark / ImgBB Key 仅通过环境变量读取，前端不再裸露密钥。
- 外部可用：Ark base_url、模型参数可配置，ImgBB 返回公网可访问的图片 URL。

## 3. 核心功能
1) 工具模板：主图优化、虚拟组套等场景的任务模板，封装调用参数与校验。  
2) 表单生成：根据模板渲染输入/下拉/多选/上传等组件，指导业务填写。  
3) 一键调用：自动组装 Prompt，调用 Ark 文生文/图生图 API，展示结果。  
4) 结果管理：输出优化后的 Prompt、生成图片，支持复制/下载。

## 4. 业务流程（卫浴主图优化示例）
1) 上传图片：主图模式 1 张；虚拟组套 2-5 张（预校验大小/格式）。  
2) 填写信息：商品类型、品牌、风格、特点或组合信息。  
3) 生成 Prompt：按模板拼装标准化提示词。  
4) 可选优化：调用 Doubao 文生文优化 Prompt。  
5) 生成图片：调用 Doubao 图生图（单图/多图），展示并可下载。

## 5. 技术方案
- 前端：React + TypeScript，Ant Design Pro 组件，Less。  
- API：火山引擎 Ark（responses 文生文、images/generations 图生图）；ImgBB 用于图片上传获取公网 URL。  
- 配置：通过环境变量注入密钥，支持 Vercel。

## 6. 环境与部署
1) 安装依赖
```bash
npm install
```
2) 配置环境变量（本地 .env / Vercel 环境变量均可）  
- ARK_API_KEY（或 VITE_ARK_API_KEY / NEXT_PUBLIC_ARK_API_KEY）  
- IMGBB_API_KEY（或 VITE_IMGBB_API_KEY / NEXT_PUBLIC_IMGBB_API_KEY）  
3) 开发预览
```bash
npm run dev
```
4) （可选）构建
```bash
npm run build
```

## 7. API 调用示例（已改为环境变量）
### 7.1 Ark 文生文（Prompt 优化）
- URL: `https://ark.cn-beijing.volces.com/api/v3/responses`
- Header: `Authorization: Bearer $ARK_API_KEY`
- 关键参数：model、input（支持 text / image+text）

### 7.2 Ark 图生图（单/多图）
- URL: `https://ark.cn-beijing.volces.com/api/v3/images/generations`
- Header: `Authorization: Bearer $ARK_API_KEY`
- 关键参数：model、prompt、image（string 或 string[]）、size、response_format

### 7.3 ImgBB 上传
- URL: `https://api.imgbb.com/1/upload`
- FormData: `image`、`key`（来自 IMGBB_API_KEY 环境变量）

## 8. 代码要点（index.tsx）
- 统一密钥读取：优先 ARK_API_KEY / IMGBB_API_KEY，兼容 VITE_ / NEXT_PUBLIC_ 前缀；缺失时抛错提示。  
- 上传前校验：大小 ≤10MB，格式限 JPG/PNG/WebP/BMP/GIF。  
- 生成前校验：图片 URL 协议与扩展名检查；单图/多图共用流程。  
- 请求头安全：Authorization 动态拼接 Bearer + 环境变量。

## 9. 已完成的安全与外部化改动
- 删除私有依赖与脚本：移除 @jd/@jdei/codmi 相关包与 build 脚本，只保留通用 lint/clean。  
- 移除私有 ESLint 规则：删除 @jd/eslint-plugin-modules-newline 及相关 rule。  
- 彻底去除明文密钥：Ark/ImgBB 调用改为环境变量注入。

## 10. 后续规划
- 扩展更多业务模板（详情页、海报等）。  
- 长任务进度与队列。  
- 结果持久化与历史版本管理。

## 11. 评审要点
- 可在 GitHub/Vercel 直接部署，无内网依赖。  
- 关键调用全走环境变量，防止泄露。  
- 提示词与调用链条标准化，业务零代码接入。